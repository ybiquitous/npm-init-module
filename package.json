{
  "version": "0.0.1",
  "name": "npm-init-module",
  "description": "A custom configuration for the `npm init` command.",
  "author": "Masafumi Koba <ybiquitous@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ybiquitous/npm-init-module.git"
  },
  "bugs": {
    "url": "https://github.com/ybiquitous/npm-init-module/issues"
  },
  "homepage": "https://github.com/ybiquitous/npm-init-module#readme",
  "engines": {
    "node": ">=6.4.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "node test.js",
    "commitmsg": "commitlint -E GIT_PARAMS",
    "lint": "npm-run-all --print-label --parallel lint:*",
    "lint:js": "eslint --ignore-path .gitignore --ext .js,.jsx,.mjs .",
    "lint:js:fix": "npm run lint:js -- --fix",
    "lint:md": "markdownlint --ignore node_modules --ignore CHANGELOG.md \"**/*.md\"",
    "fix": "prettier --write \"**/*.{md,json,yml,yaml}\"",
    "precommit": "lint-staged",
    "release": "standard-version --sign",
    "release:dry-run": "npm run release -- --dry-run"
  },
  "lint-staged": {
    "linters": {
      "*.{js,jsx,mjs}": [
        "eslint --fix --no-ignore",
        "git add"
      ],
      "*.md": [
        "prettier --write",
        "markdownlint",
        "git add"
      ],
      "*.{json,yml,yaml}": [
        "prettier --write",
        "git add"
      ]
    },
    "ignore": [
      "CHANGELOG.md"
    ]
  },
  "devDependencies": {
    "ybiq": "*"
  }
}
